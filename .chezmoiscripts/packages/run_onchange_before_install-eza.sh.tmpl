#!/bin/bash
set -eufo pipefail

{{ if eq .chezmoi.osRelease.id "debian" -}}
sudo apt-get update
sudo apt-get install -y gpg wget
sudo mkdir -p /etc/apt/keyrings
wget -qO- https://raw.githubusercontent.com/eza-community/eza/main/deb.asc | sudo gpg --dearmor -o /etc/apt/keyrings/gierens.gpg
echo "deb [signed-by=/etc/apt/keyrings/gierens.gpg] http://deb.gierens.de stable main" | sudo tee /etc/apt/sources.list.d/gierens.list
sudo chmod 644 /etc/apt/keyrings/gierens.gpg /etc/apt/sources.list.d/gierens.list
sudo apt-get update
sudo apt-get install -y eza
{{ else if or (eq .chezmoi.osRelease.id "fedora") (eq .chezmoi.osRelease.id "arch") (eq .chezmoi.osRelease.id "endeavouros") -}}
# eza is already installed via the shared package list on this distro
exit 0
{{ else -}}
echo "Unsupported distro: {{ .chezmoi.osRelease.id }}" >&2
exit 1
{{ end -}}